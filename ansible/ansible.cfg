[defaults]
# Use the inventory file in this directory
inventory = inventory.ini

# Don't create .retry files on failure
retry_files_enabled = False

# Show diff when files change (so you can see what changed)
diff_mode = True

# SSH settings - use your existing SSH config (which has "bluh" defined)
host_key_checking = False
